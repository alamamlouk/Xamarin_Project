<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Xamarin_Project.Views.Admin.Users.UpdateUser">
    <ContentPage.Content>
        <StackLayout>
            <Entry Text="{Binding Name}" x:Name="UName"/>
            <Entry Text="{Binding Username}" x:Name="UUsername"/>
            <Entry Text="{Binding password}" x:Name="UPassword"/>
            <Button Text="Update User" Clicked="Update_User"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>